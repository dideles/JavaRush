Task № 68 "Создаем двухмерный массив" (HARD)

Двухмерный массив — это массив массивов, т.е. в каждой его ячейке находится ссылка на некий массив.

Но гораздо проще его представить в виде таблицы, у которой задано количество строк (первое измерение)
и количество столбов (второе измерение).

В этой задаче мы будем создавать такой массив.

Для этого реализуй метод main(), который:
1. Считывает из консоли число N — количество строк массива (считай, что это число будет больше 0, можно не проверять).
2. Считывает N чисел из консоли (любые числа, которые будут больше 0, можно не проверять).
3. Инициализирует двухмерный массив:
3.1. количеством строк N;
3.2. строки — массивами, размер которых соответствует числам, введенным в п.2 (в порядке ввода).

Пример:
1. Введено число 5.
2. Введены числа 1, 7, 5, 9, 3.
3. Получаем такой массив:
[]
[][][][][][][]
[][][][][]
[][][][][][][][][]
[][][]


Требования:
1. В классе должна быть не объявленная публичная статическая переменная типа int[][].
2. Метод main() должен считывать числа с клавиатуры согласно условию.
3. В методе main() создай двухмерный массив согласно условию.